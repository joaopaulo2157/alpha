(()=>{'use strict';const $=s=>document.querySelector(s);const money=v=>(Number(v)||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});function store(){try{return JSON.parse(localStorage.getItem('alfa_v4_store')||'{}')}catch{return {}}}function inject(){const dash=$('#tab-dashboard');if(!dash)return;const d=store(),orders=d.orders||[],products=d.products||[],customers=d.customers||[];const pending=orders.filter(o=>!['completed','cancelled'].includes(o.status)).length,low=products.filter(p=>Number(p.stock)<=Number(d.settings?.lowStockAlert||5)).length,revenue=orders.filter(o=>o.status==='completed').reduce((a,o)=>a+Number(o.total||0),0);const box=document.createElement('div');box.className='v6-ops';box.innerHTML=`<div class="v6-op"><i class="fa-solid fa-wave-square"></i><div><span>SAÚDE OPERACIONAL</span><b>${low?'ATENÇÃO':'ESTÁVEL'}</b><small>${low} item(ns) em estoque baixo</small></div></div><div class="v6-op"><i class="fa-solid fa-headset"></i><div><span>ATENDIMENTOS ATIVOS</span><b>${pending}</b><small>pedidos ainda em andamento</small></div></div><div class="v6-op"><i class="fa-solid fa-user-group"></i><div><span>BASE DE CLIENTES</span><b>${customers.length}</b><small>clientes registrados</small></div></div><div class="v6-op"><i class="fa-solid fa-arrow-trend-up"></i><div><span>RECEITA CONCLUÍDA</span><b>${money(revenue)}</b><small>pedidos finalizados</small></div></div>`;dash.prepend(box);const live=document.createElement('span');live.className='v6-live';live.innerHTML='<i></i> PAINEL V6 ATIVO';$('.top-actions')?.prepend(live)}function pointer(){const glow=document.createElement('div');glow.className='v6-admin-glow';document.body.prepend(glow);addEventListener('pointermove',e=>{document.documentElement.style.setProperty('--ax',`${e.clientX}px`);document.documentElement.style.setProperty('--ay',`${e.clientY}px`)},{passive:true})}document.addEventListener('DOMContentLoaded',()=>{inject();pointer()})})();
